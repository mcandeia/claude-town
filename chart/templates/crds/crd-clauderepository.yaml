{{- if .Values.installCRDs | default true }}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: clauderepositories.claude-town.claude-town.io
spec:
  group: claude-town.claude-town.io
  names:
    kind: ClaudeRepository
    listKind: ClaudeRepositoryList
    plural: clauderepositories
    singular: clauderepository
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.owner
      name: Owner
      type: string
    - jsonPath: .spec.repo
      name: Repo
      type: string
    - jsonPath: .spec.repositoryPattern
      name: Pattern
      type: string
    - jsonPath: .spec.maxConcurrentTasks
      name: Max Tasks
      type: integer
    - jsonPath: .status.activeTasks
      name: Active
      type: integer
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ClaudeRepository is the Schema for the clauderepositories API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              ClaudeRepositorySpec defines the desired state of ClaudeRepository.
              Use Owner+Repo for exact match, or RepositoryPattern for regex matching.
            properties:
              labels:
                description: |-
                  Labels restricts the bot to only respond on issues with these labels.
                  Empty means all issues are allowed.
                items:
                  type: string
                type: array
              maxConcurrentTasks:
                default: 3
                description: MaxConcurrentTasks is the maximum number of concurrent
                  tasks for this repo
                minimum: 1
                type: integer
              owner:
                description: Owner is the GitHub organization or user (exact match).
                type: string
              repo:
                description: Repo is the repository name (exact match).
                type: string
              repositoryPattern:
                description: |-
                  RepositoryPattern is a regex pattern matched against "owner/repo".
                  When set, Owner and Repo fields are ignored for matching purposes.
                  Examples: "my-org/.*", "my-org/frontend-.*", ".*/my-repo"
                type: string
            type: object
          status:
            description: ClaudeRepositoryStatus defines the observed state of ClaudeRepository.
            properties:
              activeTasks:
                description: ActiveTasks is the current number of running tasks for
                  this repo
                type: integer
              conditions:
                description: Conditions represent the latest available observations
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
{{- end }}
