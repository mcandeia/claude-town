apiVersion: v1
kind: Secret
metadata:
  name: {{ include "claude-town.fullname" . }}-anthropic
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "claude-town.labels" . | nindent 4 }}
type: Opaque
stringData:
  apiKey: {{ .Values.anthropic.apiKey | quote }}
