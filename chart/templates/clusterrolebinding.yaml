apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: {{ include "claude-town.fullname" . }}
  labels:
    {{- include "claude-town.labels" . | nindent 4 }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: {{ include "claude-town.fullname" . }}
subjects:
  - kind: ServiceAccount
    name: {{ include "claude-town.fullname" . }}
    namespace: {{ .Release.Namespace }}
