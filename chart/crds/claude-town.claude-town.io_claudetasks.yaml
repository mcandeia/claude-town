---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: claudetasks.claude-town.claude-town.io
spec:
  group: claude-town.claude-town.io
  names:
    kind: ClaudeTask
    listKind: ClaudeTaskList
    plural: claudetasks
    singular: claudetask
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.repository
      name: Repository
      type: string
    - jsonPath: .spec.issue
      name: Issue
      type: integer
    - jsonPath: .spec.taskType
      name: Type
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.pullRequestURL
      name: PR
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ClaudeTask is the Schema for the claudetasks API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ClaudeTaskSpec defines the desired state of ClaudeTask.
            properties:
              branch:
                description: Branch is the git branch to work on (set for PRReviewFix)
                type: string
              issue:
                description: Issue is the GitHub issue number (0 when not associated
                  with an issue)
                minimum: 0
                type: integer
              maxClarifications:
                default: 3
                description: MaxClarifications is the maximum number of clarification
                  rounds allowed.
                maximum: 10
                minimum: 0
                type: integer
              maxIterations:
                default: 10
                description: MaxIterations is the maximum Claude iteration loops
                maximum: 50
                minimum: 1
                type: integer
              prompt:
                description: Prompt is an optional override for the Claude prompt
                type: string
              pullRequest:
                description: PullRequest is the PR number (non-zero when fixing PR
                  review)
                type: integer
              repository:
                description: Repository is the full repo name (owner/repo)
                pattern: ^[a-zA-Z0-9_.-]+/[a-zA-Z0-9_.-]+$
                type: string
              reviewCommentId:
                description: ReviewCommentID is the GitHub PR review comment ID to
                  reply to
                format: int64
                type: integer
              taskType:
                default: IssueSolve
                description: TaskType specifies what kind of task this is
                enum:
                - IssueSolve
                - PRReviewFix
                type: string
            required:
            - repository
            type: object
          status:
            description: ClaudeTaskStatus defines the observed state of ClaudeTask.
            properties:
              clarifications:
                description: Clarifications records the clarification Q&A exchanges.
                items:
                  description: ClarificationExchange records a single clarification
                    Q&A round.
                  properties:
                    answer:
                      type: string
                    question:
                      type: string
                  required:
                  - question
                  type: object
                type: array
              completionTime:
                description: CompletionTime is when the task completed
                format: date-time
                type: string
              conditions:
                description: Conditions represent the latest available observations
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              costReport:
                description: CostReport contains token usage and cost info
                properties:
                  cacheCreationInputTokens:
                    description: CacheCreationInputTokens is the total cache creation
                      input tokens used
                    format: int64
                    type: integer
                  cacheReadInputTokens:
                    description: CacheReadInputTokens is the total cache read input
                      tokens used
                    format: int64
                    type: integer
                  durationMs:
                    description: DurationMs is the total execution duration in milliseconds
                    format: int64
                    type: integer
                  estimatedCost:
                    description: EstimatedCost is the estimated cost in USD
                    type: string
                  inputTokens:
                    description: InputTokens is the total input tokens used
                    format: int64
                    type: integer
                  outputTokens:
                    description: OutputTokens is the total output tokens used
                    format: int64
                    type: integer
                type: object
              phase:
                description: Phase is the current phase of the task
                type: string
              podIP:
                description: PodIP is the IP address of the sandbox pod
                type: string
              podName:
                description: PodName is the name of the sandbox pod
                type: string
              pullRequestURL:
                description: PullRequestURL is the URL of the created PR
                type: string
              sandboxClaimName:
                description: SandboxClaimName is the name of the SandboxClaim created
                  for this task
                type: string
              startTime:
                description: StartTime is when the task started running
                format: date-time
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
